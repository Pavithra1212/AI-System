# Required in production (app will refuse to start without it)
SECRET_KEY=change-me-in-production-use-a-random-string

# Database (SQLite for dev, PostgreSQL for prod)
DATABASE_URL=sqlite:///./lost_found.db

# Set to false for production
DEBUG=true

# Server port — Railway/Render/Heroku set this automatically
PORT=8000

# CORS — comma-separated origins allowed in production
# Leave empty to allow all origins; set for security in production
# Example: ALLOWED_ORIGINS=https://myapp.up.railway.app,https://mydomain.com
ALLOWED_ORIGINS=

# AI Matching
MATCH_THRESHOLD=0.70
IMAGE_WEIGHT=0.4
TEXT_WEIGHT=0.6

# JWT token expiry (minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=480

# Max file upload size in bytes (default 5MB)
MAX_UPLOAD_SIZE=5242880
